const fakeContacts = [];

const defaultContacs = fakeContacts;

const contacts = (state = defaultContacs, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default contacts;
